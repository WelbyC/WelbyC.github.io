function setup() {
  createCanvas(500, 500);

  // set background color in grayscale:
  background(150);

  // draw ellipse
  ellipseMode(CORNER);
  ellipse(100, 100, 80, 80);

  // draw rectangle
  rect(100, 100, 80, 80)
}

function draw() {
  // no need to loop
}